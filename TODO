- tests need way to rescue if auth_allowed_ciphers is borked, some kind of config override
- tests for new mon switch to use keyring as authority
- Note that radosbench still has sessions open with osds so it won't exit
- ProtocolV2 changes to reauth or close
- Docs
- Crimson
