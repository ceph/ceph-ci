tasks:
- cephfs_test_runner:
    modules:
      - tasks.cephfs.test_misc.TestMisc.test_write_overrides_futuristic_mtime
      - tasks.cephfs.test_misc.TestMisc.test_ctime_bump_on_matching_atime

overrides:
  ceph:
    log-ignorelist:
      - evicting unresponsive client
      - POOL_APP_NOT_ENABLED
      - has not responded to cap revoke by MDS for over
      - MDS_CLIENT_LATE_RELEASE
      - responding to mclientcaps
      - RECENT_CRASH
      - MON_DOWN
