overrides:
  cephfs_test_runner:
    fs_config:
      standby_enable_host_anti_affinity: true

tasks:
- cephfs_test_runner:
    fail_on_skip: false
    modules:
      - tasks.cephfs.test_standby_affinity
