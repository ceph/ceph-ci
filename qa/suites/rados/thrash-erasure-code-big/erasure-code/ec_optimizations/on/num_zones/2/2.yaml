# Multi-zone configuration (stretched pools) for 12-OSD cluster
# Requires ec_optimizations: on
overrides:
  ceph:
    pool-config:
      num_zones: 2
      crush_map_config:
        failure_domain: osd
        zones:
          - name: zone1
            osds: [ 0, 1, 2, 3, 4, 5 ]
            monitors: [ a ]
          - name: zone2
            osds: [ 6, 7, 8, 9, 10, 11 ]
            monitors: [ b ]
        tiebreak_monitor: c
