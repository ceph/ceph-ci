[pytest]
addopts = -m 'default'
markers =
    default: Default NFS integration tests
    rate_limiting: NFS QoS configuration validation tests
    qos_enforcement: NFS QoS fio enforcement tests
    bind_monitoring: NFS bind and monitoring address tests
    colocation: NFS daemon colocation tests
