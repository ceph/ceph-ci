name: Check PR dependencies
on: [pull_request_target]
jobs:
  check_dependencies:
    runs-on: ubuntu-latest
    name: Check PR Dependencies
    steps:
    - uses: gregsdennis/dependencies-action@28d95779f2ba3e9197184e0d917984b810943f6e
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
